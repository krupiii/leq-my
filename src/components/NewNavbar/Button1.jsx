import React from 'react'

const Button = (props) => {
    return (
        <div>
            <button className='btn btn-danger'>click{ props.Krupali }</button>


        </div>
    )
}

export default Button